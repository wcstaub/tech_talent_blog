<%= form_with(model: comment, local: true) do |form| %>
  <% if comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<!-- Next 4 divs use Bootstrap Form Classes  -->
  <div class="form-group">
    <!--<%= form.text_area :user_id, value: current_user.id %> -->
    <!-- Thomas' fix -->
    <%= form.hidden_field :user_id, value: current_user.id %>
  </div>

  <div class="form-group">
    <%= form.text_area :comment_entry, placeholder: "Tell Me Something...", class: "form-control" %>
  </div>

   <!-- Next def added after post_id field was added to Comments  -->
  <div class="form-group">
    <%= form.hidden_field :post_id, value: @post.id %>
  </div>

  <div class="form-group">
    <%= form.submit "Submit", class: "btn btn-primary" %>
  </div>

<% end %>
